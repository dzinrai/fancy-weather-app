import React, { useState } from 'react';

const styles = {visibility: 'hidden', position: 'absolute'};
function Background(props) {
	const [src, setSrc] =useState(null);
	return (

		<div style={styles}>

		</div>
	);

}

export default Background;